# 决策

## 用 Go，不用 Python / Node

选 Go：编译成一个静态二进制，镜像小，空闲内存低，适合 N100 上长期挂着。页面、接口、出图都在同一个进程里，符合「一个容器一个进程」。

不选 Python：镜像和常驻内存都更大，对这个「只看图」的服务没有好处。  
不选 Node：运行时和原生缩略图依赖更重，维护面更大。

## 前端不用框架、不走构建

页面只有瀑布流和大图两件事。原生 HTML/CSS/JS 内嵌进二进制，容器里没有 `node_modules`，也不需要用户在 Unraid 上跑前端构建。

## 索引用 SQLite 文件，不用单独数据库容器

要记住路径、尺寸、修改时间，才能增量扫描和按页输出。SQLite 是一个文件，放在数据目录，重启还在。再加 Postgres/Redis 违反「能用 SQLite 就不用别的库」和「一个容器」。

## 容器内端口写死 5001，宿主机端口用环境变量

容器内地址固定，健康检查和文档不用猜测。宿主机映射写成 `${HOST_PORT:-5001}:5001`，用户只改 `.env`，不必改应用。

## 递归扫描挂载根目录，跳过隐藏路径

照片在 Unraid 上通常按年份/事件分文件夹。只扫一层会漏图。以 `.` 开头的段视为系统或缓存目录，不进相册。

## 缩略图在扫描时逐张生成，请求时补缺

瀑布流若直接出原图，手机端会卡。扫描时做缩略图，打开页面就能看。同时只做一张，避免 N100 空闲时被打满。请求时若文件还没做好，再同步补一张，避免空洞。

不做独立队列或第二个进程：单用户加图量小，函数调用足够。

## 只认 jpg / png / webp / gif，不做 HEIC

HEIC 需要额外原生库，会把「纯 Go 单二进制」打回 CGO。iPhone 可在系统里改「最兼容」或先转成 jpg。需要时再单独评估，不先加依赖。

## 列表用 id，不用文件系统路径当 URL

URL 里只出现数据库整数 id。既避免路径编码问题，也避免目录外路径被拼进 URL。取文件时仍校验落在照片根目录内。

## 大图用页面内浮层，不用跳转新页

项目要求点大图回到瀑布流。浮层不拆掉网格，滚动位置自然还在。地址栏仍带 `#p/id`，方便刷新后打开同一张，也方便手机返回键关掉大图。

## 列表用种子随机，不用每次全表打乱后 offset

随机顺序仍要游标分页：同一 `seed` 下用 `hash(seed, id)` 排序，游标是上一页最后一张的 rank+id。换一次页面就换 seed，同一轮无限滚动顺序不变。不把随机序号写入数据库，避免加图后整表重排。

`seed` 是纳秒级 `int64`，接口必须按 JSON 字符串返回。若按 JSON 数字返回会超过 JavaScript 的安全整数范围，浏览器传回第二页时发生舍入，导致排序种子变化并漏图。

## 瀑布流用虚拟滚动，不用一次性挂上全部 DOM

图片多时，CSS columns 会让几百个节点同时在页上。前端按宽高预排瀑布流，只渲染视口附近的卡片；数据仍按页用游标拉取。个人相册体量下，每页向 SQLite 取全量 id 再切片即可，不必再加队列。

## 「自动」显示模式跟 .env 时区，不跟操作系统深色开关

三档是白天、黑夜、系统自动。从未选过或选了自动时，用容器 `TZ` 的当前小时判断：6:00 到 18:00 用白天。家用相册按家里的作息比跟手机系统主题更稳。选择存在 `localStorage`。

## 登录用签名 Cookie，不用 JWT / OAuth

家里就一个账号，用户名和密码写在 `.env`。服务端用数据目录里的 `session.key` 给 Cookie 做 HMAC。没有用户表，也没有 JWT 刷新。换密码或删 `session.key` 就会全部重新登录。

`/api/health` 仍公开，否则 Compose 健康检查过不了。登录页背景需要一张图，所以单独公开 `/api/login-bg`，每次只给一张随机图，不给列表。首页、`/api/photos`、缩略图、原图都挡在登录后面；也不再按路径直接吐 `index.html`。

## Hover 只放大卡片内的图片，不放大卡片

瀑布流定位已经占用卡片的 `transform: translate(...)`。上一版再给整张卡片 `scale: 1.07`，虽然不会覆盖定位，却会压住相邻照片，视觉位移也太明显。

现在卡片的位置、尺寸和层级都不动，只让内部图片在圆角裁切范围内放大到 `1.025`，元数据用透明度淡入。这样仍有可感知的反馈，但瀑布流不会跳动或互相遮挡。

## 图形按钮内嵌 SVG，不安装 shadcn

主题和大图信息控制采用 shadcn 常见的圆角图标按钮比例与 Lucide 风格线条，但 SVG 直接写进现有 HTML。实际安装 shadcn 会同时引入 React、Tailwind 和构建链，违背当前无框架、无前端构建的架构，也会增加 Unraid 维护成本。

## 用夜樱色与一枚星标增加二次元感，不加动态装饰

界面采用紫黑、樱粉、雾紫、圆体和柔和圆角，并只在品牌标题与信息卡标题保留一枚四角星。不加飘落花瓣、粒子或角色插画，因为相册里的照片才是主角；静态视觉也避免额外 CPU 占用和手机端干扰。

## 大图信息栏桌面开、手机关

桌面右侧空得下一块说明；手机上默认摊开会挡住图。所以桌面进大图就打开，手机只留一颗「信息」按钮。点过之后按用户选择，不再跟窗口宽度抢。

## 关大图用 history.pushState，不直接改 location.hash

第一次实现里用 `location.hash = ""` 关掉大图。浏览器会把空 hash 当成跳到页顶，滚动位置丢失。改成 `history.pushState` 改地址、再手动 `scrollTo` 回到打开前的位置。手机返回键走 `popstate`，同样恢复。

## 相册从照片相对路径即时聚合，不增加相册表

每张图片的直接父目录就是相册，文件夹名就是相册名。接口读取现有有效照片后在内存中按父目录分组，并选择修改时间最新的图片作为封面；进入相册时按同一目录过滤现有照片列表。

不增加 `albums` 表：相册没有独立标题、排序或封面配置，额外表会产生目录改名后的同步和清理问题。当前单用户低并发场景下，一次线性分组更简单，也不增加后台任务。根目录没有可恢复的宿主机目录名，因此统一显示为「根目录」。

## 相册内部标识用完整相对目录，不只用文件夹名

界面按要求只展示直接文件夹名，但接口和 URL 使用完整相对目录，例如 `2026/旅行`。这样 `2025/旅行` 与 `2026/旅行` 可以分别进入，不会因重名混在一起。目录标识只用于数据库结果过滤，不直接作为文件读取路径；接口仍拒绝绝对路径和向上跳转。

## 模块切换使用普通 URL，侧栏状态只保存在浏览器

`/` 是全部照片，`/?view=albums` 是相册封面，`/?album=相对目录` 是相册内照片。模块和相册使用普通链接，刷新、收藏和浏览器返回都能自然工作；大图继续使用 `#p/id`。

侧栏展开状态写入 `localStorage`。桌面默认展开，手机默认收起；手机展开时作为覆盖抽屉，不挤窄两列瀑布流。没有新增前端路由库或状态管理框架。

## UI 用 design.md 与 tokens.css 统一，不引入在线设计依赖

主图库和登录页属于同一个应用，颜色、字体角色、间距、控件尺寸和动效规则需要保持一致。使用 `design.md` 记录设计意图，使用根目录 `tokens.css` 作为浏览器实际消费的唯一视觉令牌来源；`web/app.css` 只组合这些令牌，不再临时写散落颜色。

不引入 Tailwind、组件框架、在线字体、粒子库或动画运行时。它们会增加构建链、外网依赖和长期维护，对单用户本地相册没有收益。品牌字和中英文字体使用客户端已有字体并提供回退，断网仍可完整使用。

界面采用「月雾樱紫」：紫色负责画布层级，樱粉是唯一强调色；轻微二次元感来自字形、菱形标记和色彩关系，不用角色插画或持续装饰动画。控件改为 2–6 px 小圆角和高对比轮廓。桌面展开侧栏由 232 px 收窄到 152 px，折叠仍为 64 px；手机常驻 56 px，展开作为 152 px 覆盖抽屉。

不生成 Tailwind、DTCG 或 shadcn 兼容配置：项目没有这些运行时，保留空壳导出只会制造无效维护面。

## 标题统计视觉去单位，语义不丢失

照片页、相册页和相册内页把数量放在页面标题旁，只显示斜体艺术数字；标题固定单行，过长时省略。这样顶栏更紧凑，也避免右侧主题按钮与统计信息争抢空间。

视觉上不显示「张照片」或「个相册」，但统计节点保留带单位的 `aria-label`，读屏仍能获得完整语义。左侧 `Juen's` 与统计数字共用原艺术字体；登录页字标继续使用独立的高对比衬线体，避免改变既有登录构图。

侧栏展开宽度由品牌行决定：152 px 正好容纳 `Juen's`、品牌标记和 44 px 樱花折叠按钮；桌面 64 px、手机 56 px 的折叠宽度保持不变。主题按钮和折叠按钮使用零边框，焦点环仍保留，因此视觉更轻但键盘可达性不变。
