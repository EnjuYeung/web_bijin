# 架构

面向个人 Unraid 家庭服务器的本地图片瀑布流相册。单用户、单机、单容器、单进程。打开页面先登录。

## 技术栈

- 语言：Go。一个静态二进制同时提供页面、接口、原图和缩略图。
- 数据：SQLite 文件，和缩略图一起放在数据目录。不另起数据库容器。
- 前端：服务端内嵌的单页 HTML / CSS / JS；主样式消费根目录 `tokens.css` 的共享设计令牌，无前端框架、无构建步骤。
- 运行：一个 Docker Compose 文件、一个容器、一个进程。容器内固定监听 `5001`。宿主机端口由 Compose 环境变量 `HOST_PORT` 决定。
- 图片来源：容器内 `/photos`（只读挂载用户目录）。递归扫描子目录。

## 目录与进程

```
浏览器 ──HTTP :5001──► bijin 进程
                         │
                         ├─ 扫描 /photos（只读，递归）
                         ├─ 从照片相对路径即时聚合相册
                         └─ /data
                            ├─ bijin.db
                            ├─ session.key
                            └─ thumbs/
```

环境变量：

| 变量 | 默认 | 含义 |
|---|---|---|
| `LISTEN` | `:5001` | 容器内监听地址 |
| `PHOTOS_DIR` | `/photos` | 照片根目录 |
| `DATA_DIR` | `/data` | SQLite、缩略图、会话密钥 |
| `TZ` | `Asia/Shanghai` | 日志时间，以及日期显示 |
| `AUTH_USER` | （必填） | 登录用户名 |
| `AUTH_PASS` | （必填） | 登录密码 |

Compose 另提供 `HOST_PORT`、`PHOTOS_DIR`（宿主机路径）、`DATA_DIR`（宿主机路径）。未写 `AUTH_*` 时 Compose 默认 `juen` / `changeme`。缺账号则进程不启动。

## 功能点

1. 启动后扫描挂载目录（递归），把可显示图片写入 SQLite。
2. 先登录再进首页。用户名和密码来自 `.env`。未登录不能看瀑布流、缩略图、原图。
3. 点击缩略图看大图，再点大图回到瀑布流，保持原滚动位置。
4. 按需或扫描时生成缩略图并落盘，避免瀑布流直接拉原图。
5. 增量扫描：路径 + 大小 + 修改时间未变则跳过；文件消失则删索引和缩略图。
6. 空目录、坏图、非图片不拖垮服务。
7. 首页卡片悬停时位置和尺寸保持不变，仅在卡片裁切范围内轻微放大图片，并淡入三行元数据。
8. 大图右侧可折叠信息栏：桌面默认打开，手机默认收起。
9. 左侧导航可在「照片」和「相册」间切换；桌面默认展开，手机默认收起，状态保存在浏览器本地。
10. 「照片」保留原有全库瀑布流；「相册」按图片直接所在文件夹聚合，相册名使用文件夹名，封面左下角常驻名称和照片数量。
11. 点击相册后只浏览该文件夹的照片，继续使用原有虚拟瀑布流、大图、前后翻页和返回滚动位置逻辑。

根目录内的图片归入一个名为「根目录」的特殊相册。子目录相册以完整相对目录作为内部标识，避免不同层级的同名文件夹混淆；界面名称仍只显示直接文件夹名。相册封面取该目录中修改时间最新的可显示图片，结果稳定且无需额外配置。

支持扩展名：`.jpg` `.jpeg` `.png` `.webp` `.gif`（大小写不敏感）。路径中任一段以 `.` 开头的文件或目录跳过。

## 请求

| 路径 | 作用 |
|---|---|
| `GET /login` | 登录页（未登录可进） |
| `GET /app.css`、`GET /tokens.css`、`GET /app.js` | 登录页与相册共用的内嵌静态资源（未登录可取） |
| `POST /api/login` | 校验账号并写 Cookie |
| `GET /api/login-bg` | 登录页随机背景图（桌面横图 / 手机竖图） |
| `GET /` | 相册页面（需登录） |
| `GET /api/health` | 存活检查（不登录，给 Compose 用） |
| `GET /api/photos` | 全库分页列表；带 `album` 查询参数时只列出该相册（需登录） |
| `GET /api/albums` | 相册列表、照片数量和封面（需登录） |
| `GET /thumb/{id}` | 缩略图（需登录） |
| `GET /original/{id}` | 原图（需登录） |

照片地址只使用数据库整数 id，不把宿主机绝对路径交给浏览器。相册接口只返回照片根目录下的相对目录标识。原图与缩略图都先查库再拼路径，并校验解析后的路径仍在 `PHOTOS_DIR` 内。`/index.html` 不单独公开。

登录页背景只吐一张随机图，不提供未登录的图库列表。

## 扫描与缩略图

- 启动时扫一次；之后每隔一段时间再扫，便于用户往目录里加图后不用重启。目录遍历本身很轻，只对新增或变更的文件读尺寸。
- 缩略图在扫描时逐张生成（同时只做一张，避免打满 N100），请求时若缺失则补生成。
- 解码整图只为做缩略图；列表阶段只用文件头读宽高。
- 超大像素图不生成缩略图，避免内存尖峰；记日志，不中断其余文件。

不引入队列、后台 worker 进程或独立缩略图服务。

## 非目标

OAuth / 多用户、上传页、手工建相册、搜索、标签、HEIC/RAW、多容器、RPC、消息队列、除 SQLite 以外的数据库。
