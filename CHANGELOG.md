# 变更记录

## 2026-08-21 15:17:01 CST（标题统计与侧栏细化）

- 照片页、相册页及相册内页的数量统计移到标题旁，改为与品牌协调的斜体艺术数字；视觉取消单位，辅助技术仍能读出「张照片」或「个相册」。
- 删除顶栏中的「图库」上下文文字，照片、相册和动态相册标题统一为单行省略显示。
- 左侧 `Juen's` 恢复原手写艺术字体；桌面与手机展开侧栏进一步收窄到 152 px，桌面 64 px、手机 56 px 的折叠宽度保持不变。
- 侧栏折叠按钮改为自绘五瓣樱花图标；折叠按钮与显示模式按钮取消边框，同时保留 44 px 点击区、键盘焦点环和中文无障碍名称。
- 实际通过 JavaScript/Go 静态检查与单元测试、登录与异常接口、照片/相册/大图回归、320/375/414/768/1280 px 浏览器验收，以及 Docker 构建、启动和重启持久化检查。

## 2026-08-21 12:20:46 CST（梦幻微二次元 UI 重构）

- 使用 Hallmark 将主图库、登录页和大图层统一为「月雾樱紫」设计系统：保留真实照片作为视觉中心，以樱粉、月雾紫和小型菱形标记表达梦幻与轻微二次元感。
- 新增 `design.md` 与根目录 `tokens.css`；Go 二进制继续单进程内嵌全部前端资源，不增加前端框架、构建步骤、在线字体或动画依赖。
- 控件改为更锐利的 2–6 px 小圆角、高对比轮廓和方正图标语言；登录页改成左下角实色功能面板，大图信息改成无卡片嵌套的清晰数据行。
- 桌面展开侧栏从 232 px 收窄到 196 px，折叠为 64 px；手机常驻侧栏为 56 px，展开为 196 px 覆盖抽屉，修正抽屉被粘性顶栏遮挡的问题。
- 瀑布流间距统一为 8 px，并同步虚拟布局计算；完善 44 px 触控目标、即时焦点环、错误状态稳定占位、减少动态模式和无横向滚动保护。
- 实际通过 Go 单元测试、静态检查、Docker 构建与容器重启、登录/相册/大图流程，以及 Chrome 在 320、375、414、768、1280 px 视口下的视觉与交互验收。

## 2026-08-19 11:38:31 CST（代码文件导览）

- 新增 `CODE_MAP.md`，按后端、前端、测试、部署和项目文档分组说明现有文件职责，并提供建议阅读顺序。
- 在 `AGENTS.md` 中补充项目级 Markdown 文档的用途索引，方便首次接触项目时快速定位资料。

## 2026-08-19 00:02:38 CST（文件夹相册与左侧导航）

- 新增按图片直接所在文件夹自动聚合的相册；相册名与文件夹名一致，根目录图片归入「根目录」。
- 新增鉴权后的 `/api/albums` 接口，并让 `/api/photos` 支持按完整相对目录筛选；不同层级的同名文件夹不会混在一起。
- 新增可收缩左侧导航：「照片」保留原有全库瀑布流，「相册」展示文件夹封面；桌面默认展开，手机默认收起并以覆盖抽屉展开。
- 相册封面左下角常驻名称和照片数量；悬停仅把封面图放大 2.5%，卡片位置和尺寸不变。
- 点击封面进入相册后继续复用虚拟瀑布流、大图、前后翻页、信息栏和返回滚动位置逻辑。
- 修正随机分页 seed 超过 JavaScript 安全整数后第二页排序变化、可能漏图的问题；seed 改为字符串传输。
- 在 Intel N100 Unraid 真机的独立容器中通过单元测试、静态检查、构建、接口、桌面/手机浏览器、重启持久化和资源占用验收。

## 2026-08-18 18:40:12 CST（第三版优化）

- 收敛首页 Hover Preview：卡片不再放大、抬升或遮住相邻照片；仅内部图片轻微放大 2.5%，元数据淡入。
- 首页主题控制与大图信息控制改为 shadcn / Lucide 风格的 SVG 图形按钮，同时保留中文无障碍名称和悬停提示。
- 全局视觉调整为紫黑、樱粉、雾紫的「夜樱」色调；标题、卡片、信息栏与登录框改用更柔和的字体、圆角和阴影。
- 大图信息栏改成悬浮圆角卡片，桌面默认展开、手机默认收起的行为不变。
- 修正信息卡展开状态下折叠箭头的方向：箭头现在朝右侧屏幕边缘，与实际收起方向一致。
- 修正大图文件名样式未命中的问题，文件名现在固定显示在左下角胶囊内。

## 2026-08-18（第二版）

- 右上角显示模式改成一颗按钮，点击在白天 / 自动 / 黑夜之间切换。
- 首页卡片悬停时平滑放大，底部出现三行信息：标题、年月日、格式·分辨率·存储空间，字号随卡片大小变化。
- 大图右侧增加可折叠信息栏（文件名、格式、大小、分辨率、像素、存储空间）。桌面默认打开，手机默认收起。
- 增加登录页。账号密码来自 `.env` 的 `AUTH_USER` / `AUTH_PASS`。未登录不能进首页，也不能拉列表、缩略图、原图。
- 登录页背景随机抽一张图库照片：桌面用横图，手机用竖图。

## 2026-08-18（第一版）

- 列表改为按种子随机排序，同一轮滚动用游标分页，顺序保持稳定。
- 去掉照片白框，相邻间隙改为 4px。
- 左上角标题改为 Juen's；背景和字体按白天/黑夜两套石板灰色调重做。
- 大图增加左右按钮、方向键和滑动翻页；点大图仍可返回瀑布流。
- 首页增加白天 / 自动 / 黑夜三档，自动按 `.env` 的 `TZ` 判断（6:00–18:00 为白天），选择记在浏览器里。
- 瀑布流改为游标分页 + 虚拟滚动，视口外的卡片不挂在 DOM 上。

## 2026-08-18（首版）

- 单容器相册可启动：容器内固定监听 5001，宿主机端口用 `HOST_PORT`。
- 递归扫描挂载目录，索引写入 SQLite，增量更新，隐藏路径和非图片不进列表。
- 瀑布流页面免登录浏览，滚动加载；点图看原图，再点返回并保持滚动位置。
- 扫描时生成缩略图并落盘，瀑布流走缩略图。
- 空目录、坏图、不存在的地址、路径穿越都不会把服务打挂。
