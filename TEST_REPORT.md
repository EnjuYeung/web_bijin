# 验收记录

测试日期：2026-08-18  
环境：本机 macOS + Colima Docker，镜像 `bijin:local`，一个容器。  
照片夹：`./photos`（58 张可显示图 + 坏图/非图片/隐藏文件夹具）。

## T1 安装与启动

### T1.1 一键启动
- 功能：用 Compose 启动完整服务
- 操作：空目录下 `docker compose up -d`
- 预期：一个容器起来，端口可访问
- 验证：`docker compose ps`、浏览器/`curl`
- 状态：通过
- 证据：`bijin` 状态 `Up`，映射 `0.0.0.0:5001->5001/tcp`

### T1.2 健康检查与首页
- 功能：打开页面和健康接口
- 操作：`curl /api/health`、`curl /`
- 预期：健康接口成功，首页是相册 HTML
- 验证：`curl`
- 状态：通过
- 证据：`/api/health` 返回 200 `{"ok":true,...}`；`/` 返回 200，标题「美人」，1085 字节 HTML

### T1.3 空目录
- 功能：没图也能启动
- 操作：挂空 `./photos` 启动
- 预期：服务起来，列表为空，不崩溃
- 验证：`curl` + 日志
- 状态：通过
- 证据：`total: 0`，日志 `scan done files=0`。空状态文案在前端，此次未再截空页面（随后已放入测试图）。

## T2 核心流程

### T2.1 瀑布流
- 功能：看到瀑布流
- 操作：放入横竖方图后打开页面（桌面 1280×800、手机 390×844）
- 预期：不等高卡片，不是一行一张
- 验证：Playwright 打开并截图
- 状态：通过
- 证据：页面标题「美人」，计数「58 张」。桌面左侧竖排书名，右侧多列；手机顶栏横排、两列。网格 58 张图全部是 `/thumb/...`，0 张原图。

### T2.2 滚动加载
- 功能：图多时继续出图
- 操作：首屏约 40 张，滚到底
- 预期：加载到 58 张，不整页刷新
- 验证：Playwright `querySelectorAll('.sheet')`
- 状态：通过
- 证据：滚动前 40，滚到底后 58，最后一张 `batch/01.jpg`。接口分页：第一页 40 且 `next` 有值，第二页 18，两页 id 无重叠、合计 58。

### T2.3 看大图
- 功能：点缩略图看对应大图
- 操作：点「子目录/海边 日落.jpg」、再点 `tall.jpg`
- 预期：浮层显示对应原图
- 验证：Playwright 点击 + 地址栏 + `img.src`
- 状态：通过
- 证据：地址变为 `#p/59` / `#p/57`，浮层 `src=/original/59`（或 57），控制台 0 条错误。

### T2.4 回瀑布流并保持位置
- 功能：再点大图回到原来位置
- 操作：先滚到 `scrollY=1400`，打开 `batch/30.jpg`，再点浮层；另一次用 Escape
- 预期：浮层关掉，滚动仍在 1400
- 验证：Playwright 读 `scrollY`
- 状态：通过
- 证据：第一次实现用 `location.hash=""`，关掉后滚到 0，已修。修复后再测：`yOpen=1400`，`yAfter=1400`；Escape 后仍是 1400。手机视口点大图也能关。

### T2.5 免登录
- 功能：不登录就能看
- 操作：全新 Playwright 浏览器打开
- 预期：直接出瀑布流
- 验证：无痕等价的新会话
- 状态：通过
- 证据：无 cookie、无登录页，直接 58 张。

## T3 异常输入

### T3.1 非图片
- 功能：混入非图片不影响相册
- 操作：放入 `notes.txt`、`clip.mp4`、无扩展名文件
- 预期：不进列表
- 验证：拉全量列表比对名字
- 状态：通过
- 证据：上述文件 `present? False`

### T3.2 损坏图片
- 功能：坏 jpg 不拖垮扫描
- 操作：放入内容为文本的 `broken.jpg`
- 预期：该张不进列表，其余仍可看，日志有警告
- 验证：列表 + `docker compose logs`
- 状态：通过
- 证据：`broken.jpg present? False`；日志 `skip broken image path=broken.jpg err="image: unknown format"`；`seen=59 ready=58`

### T3.3 不存在的图片 URL
- 功能：假地址明确失败
- 操作：请求 `/thumb/9999`、`/original/9999`、`/original/abc`
- 预期：404，不是 500
- 验证：`curl -w http_code`
- 状态：通过
- 证据：三个都是 404

### T3.4 路径穿越
- 功能：读不到目录外文件
- 操作：请求 `../etc/passwd` 形式的 thumb/original/静态路径
- 预期：拒绝
- 验证：`curl`
- 状态：通过
- 证据：三个都是 404。单元测试 `TestSafeRelPath` 同样拒绝 `..` 和绝对路径。

## T4 边界

### T4.1 大图
- 功能：大照片瀑布流仍走缩略图
- 操作：4000×2800 的 `big.jpg`
- 预期：列表有宽高；缩略图远小于原图
- 验证：比下载体积
- 状态：通过
- 证据：`4000×2800`；thumb 6354 字节 / original 175594 字节，都是 JPEG 200

### T4.2 很多小图
- 功能：几十张图仍可滚动
- 操作：50 张 batch + 其它，共 58 张
- 预期：内存不明显飙
- 验证：页面滚动 + `docker stats`
- 状态：通过
- 证据：页面 58 张；空闲 CPU 0.00%，内存约 2–8 MiB

### T4.3 中文和空格文件名
- 功能：`子目录/海边 日落.jpg` 能列出、能打开
- 操作：列表查找并取原图、缩略图
- 验证：`curl` + 浏览器点击
- 状态：通过
- 证据：列表有该名字；原图/缩略图 200；浏览器点开浮层标题一致

### T4.4 横竖方图
- 功能：不同比例不错位成同一高度
- 操作：`wide.jpg` 1600×900、`tall.jpg` 700×1400、`square.jpg` 800×800
- 验证：接口宽高 + 桌面/手机截图
- 状态：通过
- 证据：接口尺寸正确；截图左列可见横图、竖图、方图高度不同

## T5 持久化

### T5.1 重启不丢索引
- 功能：重启后仍是 58 张，缩略图还在
- 操作：`docker compose restart` 后立刻拉列表
- 预期：总数不变，数据目录还在
- 验证：重启前后比 `total`，看 `data/`
- 状态：通过
- 证据：before/after total 都是 58；`data/bijin.db` 存在；`data/thumbs` 58 个文件

### T5.2 增删
- 功能：加图、删图后列表跟着变
- 操作：复制 `new-add.jpg` 后重启；再删掉重启
- 预期：59 张且包含新文件，然后回到 58 且没有它
- 验证：全量列表
- 状态：通过
- 证据：`total after add 59 has new-add.jpg True`；`total after del 58 has new-add.jpg False`；日志 `removed=1`

### T5.3 同名换图
- 功能：覆盖同路径文件后显示新图
- 操作：用 `tall.jpg` 覆盖 `square.jpg` 后重启
- 预期：体积和宽高变成竖图
- 验证：下载原图、看列表宽高
- 状态：通过
- 证据：字节 10594 → 16083；宽高变为 700×1400，id 仍为 56。随后已用 `go run genphotos.go` 恢复夹具。

## T6 回归

修复「关大图滚回顶部」后，重做 T2.1–T2.5（新浏览器会话）。瀑布流、滚动加载、点开、点回、免登录均仍通过。控制台 0 错误。

## T7 构建与部署

### T7.1 干净构建
- 功能：重新 build 再启动
- 操作：`docker compose build && docker compose up -d`
- 状态：通过
- 证据：镜像 `bijin:local` 构建成功，健康检查 200

### T7.2 单容器、容器内 5001
- 功能：一个服务；宿主机端口可改
- 操作：`HOST_PORT=5002 docker compose up -d`
- 状态：通过
- 证据：`0.0.0.0:5002->5001/tcp`；`curl :5002/` 200；`:5001` 连不上。改回 5001 后恢复。

### T7.3 空闲资源
- 功能：静置时占用低
- 操作：`docker stats --no-stream bijin`
- 状态：通过
- 证据：CPU 0.00%，内存 1.9–8.1 MiB

## 单元测试

`go test ./...` 通过（约 0.5s）：路径安全、扩展名过滤、隐藏路径、递归扫描、删文件后索引更新。

## 未测 / 限制

- 没有在 Intel N100 / Unraid 真机上跑，只在本机 ARM Colima 上跑了同一套 Compose。
- 没有用真实相机照片（夹具是色块图），解码和尺寸路径是真的。
- 没有真机 Safari / 系统返回键，手机返回用 Playwright 的 Escape / 点大图 / `popstate` 等价路径验证。
