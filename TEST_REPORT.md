# 验收记录

## 测试环境说明

- 时间：2026-08-19 00:02:38 CST
- Unraid 真机：Intel N100、约 16 GB RAM、内核 `6.18.38-Unraid`、Docker `29.5.3`
- Go 工具链：`golang:1.23-alpine` 容器；应用仍为单容器、单进程
- 隔离测试实例：`bijin-album-test-GfbjiO`，端口 `5102`，未替换或重启现有生产容器
- 测试图库：59 个文件，58 张可显示照片、1 张故意损坏的 JPEG；分布在根目录、`batch`、`misc`、`子目录` 四个相册
- 浏览器：真实 Chrome，桌面视口 `1280×800`、手机视口 `390×844`

## 测试方法说明

按安装与启动、核心正常流程、异常输入、边界条件、数据持久化、回归、构建与部署顺序执行：

- 静态与编译检查：`node --check web/app.js`、`go test -count=1 ./...`、`go vet ./...`、`go build ./...`
- 容器检查：独立镜像构建、健康接口轮询、`docker compose config -q`、容器日志、`docker stats --no-stream`
- 接口检查：使用 `curl` 实际登录并访问相册、相册内照片、根目录相册和非法目录参数
- 浏览器检查：实际登录、收缩/展开侧栏、进入相册、Hover 封面、打开/关闭大图、展开信息栏，并读取可见 DOM、坐标和计算样式
- 持久化检查：重启隔离容器后再次读取健康状态和相册页面，并核对 SQLite 与缩略图目录

## 测试结果说明

### T1 安装、单元测试、静态检查与构建

- 状态：通过。
- `node --check web/app.js` 无输出且退出码为 0。
- `go test -count=1 ./...` 输出 `ok bijin`；新增测试覆盖直接父目录分组、同名目录隔离、根目录、最新封面、目录参数校验和相册照片筛选。
- 列表接口的随机 `seed` 已由测试锁定为 JSON 字符串，避免浏览器对纳秒级 `int64` 舍入。
- `go vet ./...`、`go build ./...`、独立 Docker 镜像构建和 `docker compose config -q` 均成功。
- 健康接口最终返回 `seen=59`、`ready=58`、`scanning=false`。

### T2 相册接口、鉴权与异常输入

- 状态：通过。
- 未登录 `GET /api/albums` 返回 401，登录返回 200，非法 `album=../etc` 返回 400。
- 相册接口返回 4 个相册：`batch=50`、`misc=3`、`子目录=1`、`根目录=4`，数量合计 58。
- `album=batch` 返回 50 张且所有相对路径都以 `batch/` 开头；`album=.` 返回 4 张且都位于根目录。
- 对 `batch` 实际连续请求两页：第一页 40 张、第二页 10 张、唯一 ID 50 个、第二页 `next=null`；返回的 `seed` 类型为字符串。
- 单元测试确认 `2025/旅行` 与 `2026/旅行` 显示名都为「旅行」，但内部相册标识和照片不会混合。

### T3 桌面左侧导航

- 状态：通过。
- 1280×800 首次打开时侧栏为展开状态，宽 232 px，主内容左边界同为 232 px；「照片」「相册」文字均可见。
- 点击收起后侧栏宽 72 px，主内容左边界同步变为 72 px；两个模块名称视觉隐藏为 1×1 px，但图形按钮和无障碍名称仍保留。
- 页面 `scrollWidth=1280`，没有横向溢出；照片页仍显示 58 张并使用虚拟渲染。

### T4 相册封面与 Hover

- 状态：通过。
- 相册页显示 4 张封面，封面左下角常驻正确的文件夹名与照片数量，「相册」导航保持激活。
- 1280 px 下为四列，首张封面尺寸 `294.5×220.875` px；元数据左边和底边与封面一致，`opacity=1`。
- 悬停前封面图为 `matrix(1)`，悬停后为 `matrix(1.025)`；封面卡片的 x、y、宽、高完全不变。

### T5 进入相册、瀑布流与大图

- 状态：通过。
- 点击 `batch` 后 URL 为 `/?album=batch`，标题为 `batch`、数量为 50 张，并出现返回相册按钮。
- 相册内仍使用两页随机游标和虚拟瀑布流；真实滚动前后逐段收集到完整 50 个唯一照片 ID，网格高度从 `3121.32` px 扩展到 `3918.54` px，可见照片名均来自 `batch`。
- 桌面点击照片后 URL 增加 `#p/id`，大图、上一张/下一张和 304 px 信息栏正常；点击大图后回到 `/?album=batch`，标题、数量和滚动位置保持。

### T6 手机布局与大图信息栏

- 状态：通过。
- 390×844 下侧栏收起宽 68 px，主内容左边界 68 px；相册内瀑布流保持两列，每列 152 px，`body.scrollWidth=390`。
- 展开侧栏后形成 232 px 覆盖抽屉，模块文字可见，主内容不被再次挤窄，遮罩覆盖剩余区域。
- 相册封面页保持两列，四张封面尺寸均为 `152×114` px，无横向溢出。
- 进入 `misc` 后显示 3 张；打开大图时信息栏默认 `display=none`，点击后展开为 `304×826.40625` px，完整落在 390×844 视口内且无需内部滚动；Escape 返回 `/?album=misc`。

### T7 日志、持久化与资源占用

- 状态：通过。
- 干净浏览器标签页控制台 Errors 0、Warnings 0。一次自动化导航曾记录 Chrome 扩展的消息通道错误；新标签复核无该错误，页面代码未复现。
- 应用日志没有 `level=ERROR`。仅有故意损坏的 `broken.jpg` 被跳过警告，以及浏览器对 `/favicon.ico` 的非功能性 404。
- 隔离容器重启后健康接口立即从已有 SQLite 读到 `ready=58`，登录会话仍有效，相册页仍为 4 个相册；缩略图重启前后均为 58 个。
- 空闲快照：CPU `0.00%`，内存约 `4.984 MiB`。

## 测试结论说明

本次文件夹相册与可收缩左侧导航已在目标 Intel N100 Unraid 真机通过后端、接口、真实桌面/手机浏览器、重启持久化、构建部署和资源占用验收。分页审计中发现并修复了旧的超大整数 seed 舍入漏图问题，修复后 50 张相册已逐一滚动验证。新增功能未引入数据库表、前端框架、额外容器或额外进程；原有全部照片瀑布流、大图、登录、主题和信息栏功能未发现回归。

未测试：用户真实照片目录中的最终主观封面观感，以及数万张真实照片下的长时间滚动性能。为避免影响现有服务，本次没有替换或重启生产容器。
