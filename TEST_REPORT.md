# 验收记录

## 测试环境说明

- 时间：2026-08-21 15:17:01 CST
- 本地宿主机：macOS 26.5.2（Build 25F84）、arm64
- 工具链：Go 1.26.6、Node.js 24.11.1、npm/npx 11.6.2、Playwright CLI 0.1.18、Headless Chrome 151
- Docker：客户端 29.5.3、服务端 29.5.2；项目镜像继续使用 `golang:1.23-alpine` 多阶段构建
- 隔离实例：本地二进制监听 `127.0.0.1:5131`，Docker 测试容器监听 `127.0.0.1:5132`；未启动、替换或重启生产容器
- 测试图库：生成 63 个夹具文件；扫描器识别 59 个图片候选，其中 58 张有效、1 张故意损坏，最终聚合为 4 个相册

## 测试方法说明

按安装与启动、核心正常流程、异常输入、边界条件、数据持久化、回归、构建与部署的顺序执行：

- 静态与单元测试：`node --check web/app.js`、`go test -count=1 ./...`、`go vet ./...`、独立 `go build`、`git diff --check`
- 接口与异常路径：用 `curl` 实际提交正确/错误登录，检查未登录跳转、无效相册、健康接口和静态令牌
- 真实浏览器：用 Playwright 实际登录并操作照片页、相册页、相册内页、侧栏、主题按钮和大图层；同时读取 DOM、无障碍属性、坐标及计算样式
- 响应式与视觉：在 320、375、414、768、1280 px 宽度检查布局、点击区和横向滚动，并复核桌面与手机截图
- 容器与持久化：校验 Compose 配置，构建隔离镜像，启动并扫描测试图库，重启后再次读取同一数据目录和健康状态
- Hallmark 交付审查：执行 58 项 slop test；用户明确要求的「斜体艺术数字」与第 38a 项的通用禁用规则冲突，因此按用户要求保留并如实记录为显式覆盖

## 测试结果说明

### T1 安装、启动与基础构建

| 项目 | 记录 |
| --- | --- |
| 功能 | 应用能以现有单进程架构构建并启动 |
| 操作 | 构建隔离二进制，以临时照片和数据目录启动，等待首次扫描 |
| 预期结果 | 服务开始监听，坏图被跳过，其余图片可用 |
| 验证方式 | 进程日志、`/api/health`、Go 构建退出码 |
| 状态 | 通过 |
| 证据 | 日志显示 `scan done files=59`；健康接口返回 `seen=59`、`ready=58`、`scanning=false` |

### T2 照片、相册、主题与大图正常流程

| 项目 | 记录 |
| --- | --- |
| 功能 | 用户登录后能浏览照片和相册，并打开/关闭大图 |
| 操作 | 正确账号登录；进入照片页、相册页和 `batch` 相册；打开 `04.jpg` 后点击大图关闭；循环自动、黑夜、白天主题 |
| 预期结果 | 页面数量正确，路由和大图 hash 正常，主题三态可切换 |
| 验证方式 | Playwright 页面快照、URL、DOM 状态和按钮无障碍名称 |
| 状态 | 通过 |
| 证据 | 照片 58 张、相册 4 个、`batch` 50 张；大图 URL 加入后移除 `#p/4`；主题最终恢复为 `mode=auto` |

### T3 标题与艺术数字统计

| 项目 | 记录 |
| --- | --- |
| 功能 | 标题旁只显示协调的艺术数字，不显示单位或「图库」上下文 |
| 操作 | 分别读取照片、相册和相册内页的标题、统计文本、`aria-label` 与计算样式 |
| 预期结果 | 视觉统计只有数字；读屏保留单位；标题单行且过长时省略 |
| 验证方式 | Playwright DOM 与 `getComputedStyle` |
| 状态 | 通过 |
| 证据 | `照片 / 58 / aria-label="58 张照片"`、`相册 / 4 / aria-label="4 个相册"`、`batch / 50 / aria-label="50 张照片"`；不存在 `.page-context`；标题为 `white-space: nowrap`、`text-overflow: ellipsis`；统计字体为 `Snell Roundhand…cursive` 且 `font-style: italic` |

### T4 品牌、侧栏和无边框按钮

| 项目 | 记录 |
| --- | --- |
| 功能 | `Juen's` 恢复艺术字体，展开侧栏更窄，樱花及主题按钮没有边框 |
| 操作 | 在 1280×900 下测量展开/折叠侧栏、品牌和按钮；读取 SVG 与计算样式 |
| 预期结果 | 展开 152 px，折叠 64 px；品牌与按钮不拥挤；两个按钮为零边框并保留 44 px 点击区 |
| 验证方式 | Playwright 坐标、字体栈、边框样式、SVG 节点数 |
| 状态 | 通过 |
| 证据 | 展开侧栏 152 px，品牌文字约 80.02 px，品牌区 95 px，樱花按钮 44×44 px；折叠侧栏与内容左边界均为 64 px；两个按钮均为 `border: 0px none`；樱花 SVG 含 5 个花瓣路径和中心圆 |

### T5 手机、平板与边界宽度

| 项目 | 记录 |
| --- | --- |
| 功能 | 窄屏标题、统计、侧栏和主题按钮不重叠、不横向溢出 |
| 操作 | 在 320、375、414、768 px 宽度逐一测量；在 320 px 展开再收起抽屉 |
| 预期结果 | 手机折叠侧栏保持 56 px，平板/桌面折叠保持 64 px；展开为 152 px 覆盖抽屉；控件至少 44 px |
| 验证方式 | Playwright 视口、DOM 坐标、滚动宽度与截图 |
| 状态 | 通过 |
| 证据 | 320/375/414 px 下侧栏和内容左边界均为 56 px，768 px 下均为 64 px；各视口 `scrollWidth === clientWidth`；320 px 展开后侧栏 152 px、内容仍从 56 px 开始、遮罩为固定显示；截图见 `output/playwright/refinement-desktop-1280.png` 与 `output/playwright/refinement-mobile-320-expanded.png` |

### T6 异常输入与错误隔离

| 项目 | 记录 |
| --- | --- |
| 功能 | 未登录、错误密码、非法相册和坏图不会暴露内容或拖垮服务 |
| 操作 | 未登录请求首页；提交错误密码；认证后请求 `album=../bad`；保留一张损坏 JPEG 扫描 |
| 预期结果 | 登录门禁生效，非法相册返回 400，坏图单独跳过 |
| 验证方式 | `curl` 状态码、响应体、服务日志、浏览器控制台 |
| 状态 | 通过 |
| 证据 | 未登录 302 到 `/login?next=%2F`；错误密码 302 到 `/login?err=1`；非法相册返回 `400 {"error":"invalid album"}`；坏图只产生预期 WARN；浏览器控制台 Errors 0、Warnings 0 |

### T7 Docker、重启持久化与资源

| 项目 | 记录 |
| --- | --- |
| 功能 | 最终应用可在单容器运行，并在重启后复用索引数据 |
| 操作 | 构建 `bijin-ui-refinement-test:20260821`，挂载只读照片目录和持久数据目录，登录、检查资源，重启容器后复查 |
| 预期结果 | 首页和令牌资源可访问，重启前后都保持 58 张有效图片，无进程错误 |
| 验证方式 | Docker 构建日志、`curl`、`docker restart`、`docker logs`、`docker stats --no-stream` |
| 状态 | 通过 |
| 证据 | 首页与 `/tokens.css` 均返回 200；重启后健康接口仍为 `seen=59`、`ready=58`；日志 ERROR 0；空闲采样 CPU `0.00%`、内存 `1.988 MiB` |

### T8 最终静态回归与 Hallmark 审查

| 项目 | 记录 |
| --- | --- |
| 功能 | 最终工作区无语法、单元测试、静态分析或明显设计规范回归 |
| 操作 | 重新执行 Node/Go/差异检查，并按 Hallmark 清单审查最终界面 |
| 预期结果 | 全部工程检查通过；设计审查如有用户显式覆盖必须透明记录 |
| 验证方式 | 命令退出码、测试输出、Hallmark 58 项清单 |
| 状态 | 通过（1 项用户显式覆盖） |
| 证据 | 最终一次 `go test -count=1 ./...` 输出 `ok bijin 1.250s`；`node --check`、`go vet`、独立 `go build`、`git diff --check`、Compose 配置校验均退出 0；Hallmark 为 57/58，第 38a 项仅因用户明确要求统计数字使用斜体艺术字而保留 |

## 测试结论说明

本次标题统计与侧栏细化已通过静态检查、Go 回归、真实浏览器核心流程、五档响应式视口、异常输入、Docker 构建与重启持久化验收。用户列出的六项视觉要求均已在真实页面中确认，照片、相册、主题和大图功能未发现回归；项目仍保持单容器、单进程、无前端构建步骤。

未测试：Intel N100 Unraid 真机上的最终字体回退与渲染、用户真实照片的主观色彩观感，以及数万张真实图片下的长时间滚动性能。本次使用 macOS arm64、Colima Docker 和生成式纯色测试图片，没有影响现有生产服务。
