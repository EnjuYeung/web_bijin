# AGENTS.md

本文件适用于仓库根目录及其全部子目录。修改代码、测试、文档或部署配置前，先阅读本文件；用户在当前任务中的明确要求优先级更高。

## 项目说明

- **项目说明具有整个项目最高优先级，任何开发工作都必须围绕项目说明进行**
- 这是一个面向个人 Unraid 家庭服务器的本地图片瀑布流相册。最基本使用流程是，用户用手机或桌面打开前端，登录后即可浏览瀑布流，点击图片可查看大图，点击大图可返回瀑布流。
- 单用户、低并发使用。
- 用户不会写代码，也不会测试，用户可接受一键命令式的调试工作。

## 用户约束

- 目标环境固定为 Intel N100、16 GB RAM 的 Unraid 家庭服务器。
- 优先低空闲 CPU、低内存、低维护，而不是横向扩展能力。
- 必须保持单机、本地化架构。能用函数调用就不引入 RPC，能同步完成就不引入队列，能使用 SQLite 就不增加数据库容器。
- 默认只使用一个 Compose 文件、一个容器和一个应用进程；`docker compose up -d` 应能启动完整服务。
- 这是个人私有项目，简单鉴权即可，不增加 OAuth、OIDC、JWT 刷新、RBAC、多租户、用户系统、复杂 Session 或 API Key 管理平台。
- 优先删除失去意义的代码和配置，不为假设中的未来扩展保留分布式兼容层或空壳抽象。
- GitHub 默认且唯一长期分支是 `master`；未经用户明确要求，不创建或推送额外长期分支。
- 项目开始开发前，将技术栈、功能点、架构写入项目级的 `ARCHITECTURE.md`，若架构变动，要更新`ARCHITECTURE.md`，始终保持简洁、可读、可追溯。
- 每完成一个功能点且测试通过后，将进展写入项目级的 `CHANGELOG.md`，保持简洁、可读、可追溯。
- 每完成一个关键技术、架构或设计决策，写入项目级的 `DECISIONS.md`，记录为什么选 A 不选 B，保持简洁、可读、可追溯。
- 改动`AGENTS.md`，必须在 PR 中说明改动原因且需用户批准。
- 测试结果写写入`TEST_REPORT.md`，保持简洁、可读、可追溯，保留最新一次的测试结论即可。测试报告必须包含：
  - 测试环境说明
  - 测试方法说明
  - 测试结果说明
  - 测试结论说明
- 如果因为环境限制无法测试，必须并说明具体原因。

## 禁止事项

- 没有实际运行就说测试通过
- 因为代码能编译就认为功能正确
- 因为接口返回 200 就认为业务正确
- 修改测试代码来迎合错误实现
- 删除失败测试以使测试套件变绿
- 忽略错误日志
- 隐瞒未测试项目
- 使用“应该可以”“理论上没问题”代替实际验证